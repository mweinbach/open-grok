[package]
license = "Apache-2.0"
name = "xai-grok-status-line"
version = "0.1.0"
edition.workspace = true
description = "Status-line configuration and client context contract"

[dependencies]
serde = { workspace = true, features = ["derive"] }
strum = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
toml = { workspace = true }

[features]
test-support = []
default-bazel = ["test-support"]

[lints]
workspace = true
